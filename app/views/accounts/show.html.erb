<p style="color: green"><%= notice %></p>

<%= render @account %>

<div>
  <%= link_to t("accounts.show.edit"), edit_account_path(@account) %> |
  <%= link_to t("accounts.show.back"), accounts_path %>

  <%= button_to t("accounts.show.destroy"), @account, method: :delete %>
</div>
