<%= form_with(model: invoice) do |form| %>
  <% if invoice.errors.any? %>
    <div style="color: red">
      <h2>
        <%= t(
              "invoices.form.errors_title",
              count: invoice.errors.count
            ) %>
      </h2>

      <ul>
        <% invoice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :total_amount,
          t("invoices.attributes.total_amount"),
          style: "display: block" %>
    <%= form.text_field :total_amount %>
  </div>

  <div>
    <%= form.label :due_date,
          t("invoices.attributes.due_date"),
          style: "display: block" %>
    <%= form.date_field :due_date %>
  </div>

  <div>
    <%= form.label :status,
          t("invoices.attributes.status"),
          style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.label :account_id,
          t("invoices.attributes.account"),
          style: "display: block" %>
    <%= form.text_field :account_id %>
  </div>

  <div>
    <%= form.submit t("invoices.form.submit") %>
  </div>
<% end %>
