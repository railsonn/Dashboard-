<p style="color: green"><%= notice %></p>

<%= render @invoice %>

<div>
  <%= link_to t("invoices.show.edit"), edit_invoice_path(@invoice) %> |
  <%= link_to t("invoices.show.back"), invoices_path %>

  <%= button_to t("invoices.show.destroy"), @invoice, method: :delete %>
</div>
