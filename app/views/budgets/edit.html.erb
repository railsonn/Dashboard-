<% content_for :title, t("budgets.edit.title") %>

<div class="container-fluid py-4 <%= @theme %>-settings budget-edit-page">
  <div class="row justify-content-center">
    <div class="col-lg-8 col-xl-6">

      <!-- Título -->
      <h4 id="budget-edit-title" class="mb-4">
        <%= t("budgets.edit.heading") %>
      </h4>

      <!-- Formulário -->
      <%= render "form", budget: @budget %>
      
    </div>
  </div>
</div>
