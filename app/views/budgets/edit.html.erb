<% content_for :title, t("budgets.edit.title") %>

<h1><%= t("budgets.edit.heading") %></h1>

<%= render "form", budget: @budget %>

<br>

<div>
  <%= link_to t("budgets.edit.show"), @budget %> |
  <%= link_to t("budgets.edit.back"), budgets_path %>
</div>
