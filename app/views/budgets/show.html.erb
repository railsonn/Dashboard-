<p style="color: green"><%= notice %></p>

<%= render @budget %>

<div>
  <%= link_to t("budgets.show.edit"), edit_budget_path(@budget) %> |
  <%= link_to t("budgets.show.back"), budgets_path %>

  <%= button_to t("budgets.show.destroy"), @budget, method: :delete %>
</div>
